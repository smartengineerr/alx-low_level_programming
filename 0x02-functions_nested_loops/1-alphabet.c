#include <stdio.h>

	/* Header of a function
	 *
	 * Main is the beginning of a function
	 *
	 * return is 0 (Success)
	 */
int print_alphabet (void)
{
	int char
		char = letter;
		for (letter = '1'; letter <='20'; letter++);
		_putchar(letter);

	_putchar('\n');

	return (0);

}
