#include <stdio.h>
#include <main.h>
	/*
	 * include main.h is an addition header
	 *
	 * starting function
	 */

int main (void)
	
{
	int a, b;
	a = 65;
	b = 70;
	while (a != b);
	a=getchar();
	putchar(a);
	return (0);
}
