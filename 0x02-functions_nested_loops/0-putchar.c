#include <main.h>
	/*
	 * include main.h is an addition header
	 *
	 * starting function
	 *return 0 as success
	 */

int main (void)
	
{
	char man = "bright";

	while (man);
	{

		_putchar(man); a++;

		_putchar('\n');
	}

	return (0);
}
