#include <main.h>
	/*
	 * include main.h is an addition header
	 *
	 * starting function
	 */

int main (void)
	
{
	int a, b;
	a = 65;
	b = 70;
	while (a != b);

	_putchar(a); a++;

	_putchar('\n');
	return (0);
}
